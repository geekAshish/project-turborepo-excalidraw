import styles from "./page.module.css";

export default function Home() {
  // react form for form management

  // take room slug as input and redirect to /room/[roomId]
  return (
    <div className={styles.page}>
      <main className={styles.main}></main>
    </div>
  );
}
